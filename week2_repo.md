# 周计划与进度报告（第2周：2026-03-09 ~ 2026-03-13）

## 一、第2周报告

### 1. 达成的里程碑

1. 完成核心能力验证（非代码线）
   - `Week2/SKP(additional).py` 验证了Gemini-2.5链路（基础prompt、顺序链、结构化输出、路由链、RAG检索）。
   - `Week2/doc_prac_with_claude.ipynb` 验证了文档检索向量库（CSVLoader + VectorstoreIndexCreator + InMemoryVectorStore）对业务问答场景的可行性。
2. 业务与管理工作推进
   - 梳理第2个月设计阶段交付：BRD、架构、数据模型、接口规范、安全合规、风险管理、KPI。
   - 完成需求访谈模板，整理“采购-发票-融资-交付”四个核心场景与优先级。
   - 起草项目管理仪表盘：16个代理进度、风险登记、资源与时间消耗。
3. 文档与风险管理交付
   - 输出本周报告结构，含里程碑、未达成、计划调整、风险与对策。
   - 建立风险登记初版（风险ID/可能性/影响/缓解/责任人）。

### 2. 未达成的里程碑

1. BRD 尚未完成全量详细版本（采购订单、发票匹配、风险预警、现金流预测代理需补齐）。
2. Connex API 接口规范尚未形成（请求/响应/错误码/认证/版本管理缺少统一文档）。
3. 安全与合规审查还未闭环（PII标记、审计日志、访问控制、安全测试需完成）。
4. 评估体系未落地（量化KPI、评测指标、回归测试计划尚未完成）。

未达成原因：
- 第2周工作重心偏向需求对齐与方案确认以及继续自主练习，编码与集成迭代为次要。
- 跨团队评审流程需更多业务+合规投入，导致文档出炉周期拉长。

解决方案：
- 第3周启动文档集中评审会，明确迭代节点（BRD v1.0：3/18 提交；API/安全清单：3/20 提交）。
- 建立每周2次检查点，产品+业务+架构+合规同步状态并记录变更。

### 3. 计划与工作量调整

- 2026-03-11（背景：Week2 发现多模型对标需求）
  决策结果：将 `LangChain` 栈下游引擎选型从“仅 OpenAI”扩展为“Google/Anthropic/自定义”，并增加 `SKP(additional)` 端到端巡检。
- 2026-03-13（背景：需求文档需求上升）
  决策结果：第3周新增“BRD+API 规范撰写”任务（最少5页文档），并同步到项目 `repo`。

### 4. 可预见风险及应对

1. 风险：多模型/多库版本兼容性爆炸导致重复调试
   - 应对：固定 `langchain`、`langchain-google-genai`、`langchain-anthropic` 的版本，设置 `requirements.txt` 并锁定测试。
2. 风险：模型 key/额度不足导致单机验证中断
   - 应对：准备备用 key，使用伪嵌入 `FakeEmbeddings` 作为离线能力验证工具。
3. 风险：核心业务流程未纳入（采购-发票-融资）导致 PoC 与产品脱节
   - 应对：与业务部门约 3/16 需求确认会，明确 3 个核心场景、API 输入/输出、成功判定。

## 二、第3周计划（2026-03-16 ~ 2026-03-20）

### 1. 工作分配

- 业务&产品：完成 BRD（采购订单、发票匹配、风险预警、现金流预测 4 个子代理）。
- 工程：推进模块化能力框架设计（已从 Notebook 向独立组件拆分，但统一入口留到后续阶段）。
- 工程：完善 `SKP(additional)` 关键能力验证（路由链、向量检索、工具调用）并输出测试结果。
- 数据：构建验证集（100 条问答、50 条检索、20 条工具调用失败用例）。
- 测试：设计并执行回归测试，记录关键指标（成功率/响应时间/一致性）。

### 2. 预期达成的里程碑

1. 完成模块化设计文档（包含组件职责、接口定义、运维/追踪要点），暂不以统一pipeline为第一目标。
2. 完成 `Week2` 端到端演示并记录录像或 PPT（含一个业务用例）。
3. 形成阶段性文档：`Week2_BRD.md`、`Week2_Test_Result.md`。
4. 提交项目周报给导师并同步到 LumiNUS 仪表盘。

## 三、附：项目整体计划/进度对照

- 当前进度：第1周原型验证已完成，第2周完成模型验证与需求框架搭建。  
- 第2周重点在业务需求与设计对齐、文档规范与风险管控，技术验证为并行任务。  
- 下一阶段（第3周）目标：进入冲刺1开发，优先实现发票匹配、智能询价、采购订单自动生成代理。

